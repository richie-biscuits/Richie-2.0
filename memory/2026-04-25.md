
## Orthogonal Setup (Apr 25)
- Installed Orthogonal CLI: `@orth/cli` via npm global; binary symlinked to `/Users/openclaw_admin/.local/bin/orth` so `orth` is on PATH.
- Authenticated Orthogonal with Marrs-provided API key; `orth whoami` confirms authenticated, balance shows `$5.00`.
- Core Orthogonal skills installed for OpenClaw and Gemini:
  - `/Users/openclaw_admin/.openclaw/skills/orthogonal-find-skill/SKILL.md`
  - `/Users/openclaw_admin/.openclaw/skills/orthogonal-find-api/SKILL.md`
- Stored credentials in Orthogonal CLI config and locked file permissions to 600; also wrote docs-compatible `~/.config/orthogonal/credentials.json` with 600 permissions.
- Verified `orth api search "web scraping"` works and returns marketplace APIs.
- Note: new OpenClaw skills may require a fresh session/gateway reload to appear in injected available skills list.
